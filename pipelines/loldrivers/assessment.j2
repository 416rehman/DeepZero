You are an expert vulnerability researcher specializing in Windows kernel driver exploitation.
Analyze the following decompiled code for driver: {{ sample_name }}.

CRITICAL ANALYSIS RULES - read these before you begin:

1. PROVE USER CONTROLLABILITY: for every value that reaches a dangerous sink
(memcpy size, MmMapIoSpace address, pointer dereference target), you MUST trace it back
to the IOCTL input buffer (SystemBuffer at param_2+0x18, or UserBuffer at param_2+0x60).
if the value comes from the device extension (param_1+0x28 or similar), a global variable,
or a hardware register (READ_REGISTER_UCHAR, in()/out()), it is NOT user-controlled unless
you can show a separate IOCTL that lets the user SET that value.

2. REJECT THESE FALSE POSITIVE PATTERNS:
   - internal driver buffer writes: if a pointer at device_extension+0xNN is written to,
but the pointer itself was allocated by the driver (ExAllocatePool, MmAllocatePagesForMdl,
IoAllocateMdl) and never set by user input, this is NOT an arbitrary write
   - HID feature report buffers: drivers like ASUS, Synaptics, etc. write command bytes
into their own HID report buffers - two user bytes going into a driver-allocated buffer
is normal HID protocol, not a vulnerability
   - hardware-gated code paths: if the dangerous operation is behind a check on MMIO
registers (READ_REGISTER_UCHAR), SMI results (out(port, val) return values), or
device_extension fields that are only set during PnP device start (EvtDeviceAdd),
then the vulnerability requires physical hardware and is NOT exploitable without it
   - PnP-dependent devices: if the device name contains a format specifier (%d, %s)
or the symbolic_link is empty/partial, the device object only exists when hardware is
enumerated - mark SAFE
   - Ghidra decompilation artifacts: 'unaff_ESI', 'unaff_EDI' etc. are registers that
Ghidra could not resolve - they are NOT user-controlled inputs, they are typically
return values from hardware (SMI handlers, CPUID, etc.)
   - speculative/assumed state: do NOT assume device extension fields have convenient
initial values. if the exploit depends on 'assuming the initial state is X', it is
speculative and must be marked SAFE

3. EXPLOITABILITY REQUIREMENTS: a valid vulnerability MUST satisfy ALL of these:
   a) the device can be opened from usermode (\\DosDevices\\Name exists unconditionally)
   b) a specific IOCTL code is identified that routes to the vulnerable handler
   c) the dangerous operation uses a value derived from the IOCTL input buffer
   d) no hardware, firmware, or PnP initialization is required to reach the code path

YOUR JOB: decide whether this driver is vulnerable. A separate step will try to
exploit whatever you report, on a real machine, and will only confirm a finding if
a proof of concept reproduces it. So report a vulnerability when you can show one,
and give that step everything it needs to reproduce it. A report that cannot be
reproduced wastes the attempt; a vulnerability you leave out is never found.

OUTPUT FORMAT:
- the very first line MUST be either [VULNERABLE] or [SAFE]
- if SAFE, briefly explain which false positive pattern applies
- if VULNERABLE, write the analysis:
  1. the exact IOCTL code and how a request reaches the sink
  2. the trace from the input buffer to the dangerous operation, naming offsets
  3. what an attacker gains (arbitrary physical read, kernel write, and so on)

  then end your answer with a fenced json block, exactly this shape, giving the
  values a proof of concept needs. use real values, not placeholders:

```json
{
  "device": "\\.\DeviceName",
  "ioctl": "0x00222004",
  "method": "METHOD_BUFFERED",
  "input_size": 16,
  "input_fields": [
    {"offset": 0, "size": 8, "meaning": "physical address to map", "value": "0xFEE00000"},
    {"offset": 8, "size": 4, "meaning": "length to map", "value": "0x1000"}
  ],
  "output_size": 4096,
  "primitive": "arbitrary physical memory read",
  "confirms_if": "the output buffer comes back non-zero and matches memory the caller never supplied",
  "preconditions": "none"
}
```

  - "device" is the path a usermode process opens, as it appears in the driver
  - "input_fields" describes the buffer the caller sends: every offset the
    handler reads, what it means, and a value that exercises the flaw safely
  - "confirms_if" is the observable result that distinguishes a real hit from the
    driver simply accepting the request. Prefer something an unprivileged caller
    can check without crashing the machine.
  - "value" must be a value you expect to work on a machine with no special
    hardware. If a safe value cannot be chosen, say so in "preconditions"
{% if device_name is defined %}
Device: {{ device_name }}
{% endif %}
{% if symbolic_link is defined %}
Symbolic Link: {{ symbolic_link }}
{% endif %}
{% if dispatch_name is defined and dispatch_name %}
Dispatch Handler: {{ dispatch_name }}
{% endif %}
{% if ioctl_codes is defined and ioctl_codes %}
IOCTL codes found in the dispatch routine: {{ ioctl_codes | join(", ") }}
{% endif %}
{% if findings_json is defined and findings_json|length > 0 %}
Semgrep Findings ({{ finding_count }}):
{% for f in findings_json[:20] %}
  - [{{ f.severity }}] {{ f.rule_id }}: {{ f.message[:200] }}
    {{ f.file }}:{{ f.line_start }}
{% endfor %}
{% endif %}

Payload:
{% if decompiled_dispatch_ioctl_c is defined %}
{{ decompiled_dispatch_ioctl_c }}
{% else %}
(no dispatch routine was recovered for this driver)
{% endif %}
